# They projects use cargo

for create à new project :

```bash
cargo new project_name
```

for complie and run the project :

```bash
cargo run
```

for complie the project :

```bash
cargo build
```
